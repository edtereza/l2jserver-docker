#!/bin/sh
# ---------------------------------------------------------------------------
# Query Check
# ---------------------------------------------------------------------------
IS_READY=$(ps aux | grep java | grep '.jar' | awk '{print $2}')
if [[ "$IS_READY" == "root" ]];
    then
        exit 0
    fi
exit 1